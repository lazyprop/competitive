#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int cost[10] = {0, 2, 5, 5, 4, 5, 6, 3, 7, 6}:

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int n, m; cin >> n >> m;
    int a[m];
    for (int i = 0; i < m; i++) cin >> a[i];
}
