#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    int n, maxt; cin >> n >> maxt;
    pair<int, int> a[n+1];
    for (int i = 1; i <= n; i++) {
        cin >> a[i].second >> a[i].first;
    }
    sort(a+1, a+n+1);

}
