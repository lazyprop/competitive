#include <bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
	freopen("in.txt", "r", stdin);

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int T; cin >> T;
    while (T--) {

    }
}
